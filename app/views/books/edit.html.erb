<body>
  

  <!--form_with(model: @book)の部分は、どのモデルを編集するかを、model: @bookを引数として渡してあげることで、指定しています-->
  <!--local: trueは、form_withのデフォルトである非同期通信でデータを送信するのをキャンセルしています！-->

  <main>
    <p id="notice"></p>
    <div class="container">
      <div class="row">
        <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
        <h1>Editing Book</h1>
        
        <%= render 'books/alert', book: @book %>
        
        <%= form_with model:@book,local:true do |f| %>

        <div class="form-group">
        <%= f.label :title, "Title" %>
        <%= f.text_field :title, class: "form-control book_title" %>
          <!--text_fieldは、1行のみの入力フォームを作成-->
        </div>

        <div class="form-group">
        <%= f.label :body, "Opinion" %>
        <%= f.text_area :body, class: "form-control book_body" %>
        <!--text_areaは、複数行入力可能な入力フォームを作成-->
        </div>

        <div>
        <%= f.submit 'Update Book', class:"btn btn-success"%>
        <!--submitは、上で入力されたデータを送信先に送信-->
        <!--date: {confirm: }を指定することで、ボタンを押した時にメッセージが出力されるようになります！-->
        </div>
        <% end %>
  
        <%= link_to book_path(@book), class:"book_18308" do %>
        Show
        <% end %>
        |
        <%= link_to books_path, class:"back" do %>
        Back
        <% end %>
        </div>
      </div>
    </div>
 

  </main>
  
</body>  
